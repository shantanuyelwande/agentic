# Required: Anthropic API key (Claude is used via langchain-anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the Claude model
# CLAUDE_MODEL=claude-sonnet-4-6

# Optional: orchestrator port (default 8001 to avoid clash with the agentic repo)
# PORT=8001
